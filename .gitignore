# 此为注释 ,将被 Git 忽略
# 忽略所有 .iml 结尾的文件
*.iml
# 忽略 classes/ 目录下的所有文件
classes/
# 忽略目录 build 下的全部内容；注意，不管是根目录下的 /build/ 目录，还是某个子目录 /build/xxx/ 目录，都会被忽略；
**/build/
# 忽略根目录下的 /.idea/ 目录的全部内容；
/.idea/*
# 忽略根目录下的 /.gradle/ 目录的全部内容；
/.gradle/*
# 忽略根目录下的 /.git/ 目录的全部内容；
/.git/*
# 忽略 logs/ 目录下的所有文件
logs/
/out/
/.gradle/
/.idea/
